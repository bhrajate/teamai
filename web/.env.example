# 前端构建期变量。复制为 .env.local 后按需改；均非敏感项。
# Admin API 令牌不在此列 —— 它在运行时由「设置」页写进 localStorage，
# 不打进构建产物（打进去等于把令牌发给每个访客）。

# API 基址。留空即用相对路径 /api：
#   - dev：vite proxy 代到后端，同源
#   - 生产同源部署（前端与 API 同域）：同样留空即可
# 独立静态站且 API 在别的域时，填完整前缀，例如 https://teamai-api.example.com
VITE_API_BASE_URL=

# dev server 的代理目标，仅 npm run dev 生效
VITE_DEV_API_TARGET=http://127.0.0.1:8000
